<h2>Helm Chart</h2>
What is the Helm Chart?
- Helm is a package manager for kubernetes. like apt for ubuntu and npm for nodejs.


nginx-heml/
├── Chart.yaml
├── templates
│ ├── deployment.yaml
│ └── service.yaml
└── values.yaml

